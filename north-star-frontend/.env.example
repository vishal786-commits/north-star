# Base URL the frontend calls for the API.
# Leave as /api in dev (Vite proxies it to the backend) and in prod (nginx proxies it).
# Only override if you host the API on a totally separate domain.
VITE_API_BASE_URL=/api
